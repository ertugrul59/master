npm init -y

npm i express ejs body-parser

lvel1 : sadece database e veri yollar alır herhangibi bir gizleme yapmaz.

lvel2 :

npm i mongoose-encryption // bu package i npmden indirdik.
var encrypt = require('mongoose-encryption'); // level2

const secret = "Thisisourlittlesecret."; // level2  secret is an encryption key whic is "Thisisourlittlesecret."
userSchema.plugin(encrypt, { secret: secret, encryptedFields: ["password"]}); //lvel2

lvel3 :

touch .env // bu dosyayı olustuk

const secret = "Thisisourlittlesecret."; //  app.jsden aldıgımız bunu değiştirerek .env ye taşıdık.

userSchema.plugin(encrypt, { secret: process.env.SECRET, encryptedFields: ["password"]});

touch gitignore // https://github.com/github/gitignore/blob/master/Node.gitignore
